import React from 'react';
import { BarChart } from '../../../index';

export default React.createClass({
	render() {
		return (
			<BarChart
				data={[]}

				yAxisTitle='Revenue'
			/>
		);
	},
});
