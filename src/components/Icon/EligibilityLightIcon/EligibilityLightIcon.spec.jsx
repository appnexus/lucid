import { icons, common } from '../../../util/generic-tests';

import EligibilityLightIcon from './EligibilityLightIcon';

describe('EligibilityLightIcon', () => {
	common(EligibilityLightIcon);
	icons(EligibilityLightIcon);
});
