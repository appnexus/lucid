import { icons, common } from '../../../util/generic-tests';

import BackUpArrowIcon from './BackUpArrowIcon';

describe('BackUpArrowIcon', () => {
	common(BackUpArrowIcon);
	icons(BackUpArrowIcon);
});
