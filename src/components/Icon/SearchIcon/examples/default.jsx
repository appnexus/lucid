import React from 'react';
import { SearchIcon } from '../../../../index';

export default React.createClass({
	render() {
		return (
			<div>
				<SearchIcon />
				<SearchIcon isBadge />
			</div>
		);
	}
});
