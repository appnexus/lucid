import React from 'react';
import SearchIcon from '../SearchIcon';

export default React.createClass({
	render() {
		return (
			<div>
				<SearchIcon />
				<SearchIcon badge />
			</div>
		);
	}
});
