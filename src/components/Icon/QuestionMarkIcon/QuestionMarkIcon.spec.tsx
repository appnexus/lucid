import { icons, common } from '../../../util/generic-tests';

import QuestionMarkIcon from './QuestionMarkIcon';

describe('QuestionMarkIcon', () => {
	common(QuestionMarkIcon);
	icons(QuestionMarkIcon);
});
