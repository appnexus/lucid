import { icons, common } from '../../../util/generic-tests';

import StarOutlineIcon from './StarOutlineIcon';

describe('StarOutlineIcon', () => {
	common(StarOutlineIcon);
	icons(StarOutlineIcon);
});
