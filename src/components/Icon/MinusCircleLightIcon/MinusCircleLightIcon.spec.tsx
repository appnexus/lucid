import { icons, common } from '../../../util/generic-tests';

import MinusCircleLightIcon from './MinusCircleLightIcon';

describe('MinusCircleLightIcon', () => {
	common(MinusCircleLightIcon);
	icons(MinusCircleLightIcon);
});
