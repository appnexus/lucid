import { icons, common } from '../../../util/generic-tests';

import VideoOnDemandIcon from './VideoOnDemandIcon';

describe('VideoOnDemandIcon', () => {
	common(VideoOnDemandIcon);
	icons(VideoOnDemandIcon);
});
