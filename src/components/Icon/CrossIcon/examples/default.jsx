import React from 'react';
import { CrossIcon } from '../../../../index';

export default React.createClass({
	render() {
		return (
			<div>
				<CrossIcon />
				<CrossIcon isBadge />
			</div>
		);
	},
});
