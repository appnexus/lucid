import { icons, common } from '../../../util/generic-tests';

import QuestionMarkCircleIcon from './QuestionMarkCircleIcon';

describe('QuestionMarkCircleIcon', () => {
	common(QuestionMarkCircleIcon);
	icons(QuestionMarkCircleIcon);
});
