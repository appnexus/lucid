import { icons, common } from '../../../util/generic-tests';

import ShoppingCartIcon from './ShoppingCartIcon';

describe('ShoppingCartIcon', () => {
	common(ShoppingCartIcon);
	icons(ShoppingCartIcon);
});
