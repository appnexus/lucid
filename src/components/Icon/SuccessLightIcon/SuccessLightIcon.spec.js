import { icons, common } from '../../../util/generic-tests';

import SuccessLightIcon from './SuccessLightIcon';

describe('SuccessLightIcon', () => {
	common(SuccessLightIcon);
	icons(SuccessLightIcon);
});
