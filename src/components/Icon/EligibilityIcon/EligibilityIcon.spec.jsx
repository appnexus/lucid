import { icons, common } from '../../../util/generic-tests';

import EligibilityIcon from './EligibilityIcon';

describe('EligibilityIcon', () => {
	common(EligibilityIcon);
	icons(EligibilityIcon);
});
