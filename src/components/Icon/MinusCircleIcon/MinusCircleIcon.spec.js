import { icons, common } from '../../../util/generic-tests';

import MinusCircleIcon from './MinusCircleIcon';

describe('MinusCircleIcon', () => {
	common(MinusCircleIcon);
	icons(MinusCircleIcon);
});
