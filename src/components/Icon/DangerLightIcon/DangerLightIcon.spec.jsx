import { icons, common } from '../../../util/generic-tests';

import DangerLightIcon from './DangerLightIcon';

describe('DangerLightIcon', () => {
	common(DangerLightIcon);
	icons(DangerLightIcon);
});
